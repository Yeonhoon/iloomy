<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="items">
	<select id="selectByPk" parameterType="int" resultType="item"> 
		select items_no, items_name, items_company, items_price,items_context, items_color, items_detail, items_stock, items_model, items_manufacture, items_origin, items_attachtype, items_attachoname, items_attachsname
		from items
		where items_no = #{no}

	</select>
	
	<insert id="insert" parameterType="item">
		insert into items
			(items_no, items_name, items_company, items_price, items_context, items_color, items_detail, items_stock,
			items_model, items_manufacture, items_origin, items_attachoname, items_attachsname, items_attachtype)
		values 
			(items_seq.nextval, #{itemsName}, #{itemsCompany}, #{itemsPrice}, #{itemsContext}, #{itemsColor}, #{itemsDetail}, #{itemsStock},
			#{itemsModel}, #{itemsManufacture}, #{itemsOrigin}, #{itemsAttachOname}, #{itemsAttachSname}, #{itemsAttachtype})
	</insert>
	
</mapper>