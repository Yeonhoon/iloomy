<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="items">
	<select id="selectByPk" parameterType="int" resultType="item"> 
		select items_no, items_name, items_company, items_price,items_context, items_color, items_detail, items_model, items_manufacture, items_origin, items_mainphoto
		from items
		where items_no = #{no}

	</select>
	
	<insert id="insert" parameterType="item">
		<selectKey keyProperty = "ono" resultType="int" order="BEFORE">
			select items_seq.nextval from dual
		</selectKey>
		insert into items
		(items_no, items_name, items_company, items_price, items_context, items_color, items_detail, items_model, items_manufacture, items_origin, items_mainphoto)
		values (#{ono}, #{itemsName}, #{itemsCompany}, #{itemsPrice}, #{itemsContext}, #{itemsColor}, #{itemsDetail}, #{itemsModel}, #{itemsManufacture}, #{itemsOrigin}, #{itemsMainphoto})
	</insert>
	
</mapper>